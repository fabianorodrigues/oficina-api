apiVersion: v1
kind: Service
metadata:
  name: oficina-api
  namespace: oficina
  labels:
    app: oficina-api
spec:
  type: NodePort
  selector:
    app: oficina-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: __API_NODE_PORT__
